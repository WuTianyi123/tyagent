fix: address low-severity review findings

Changes:
1. Remove dead code _MARKDOWN_FENCE_OPEN_RE / _MARKDOWN_FENCE_CLOSE_RE
2. Tighten _TABLE_BLOCK_RE regex from .+ to (?:[^|\n]+\|)+ for precision
3. Add fence+table combination tests: table after code block, code block
   after table, fence interaction with _build_markdown_post_rows